#! /usr/bin/env bash

# Installs applications.

install_bare_pkg "$ELM_APP_URL" "$ELM_APP_NAME"
install_dmg_app "$DOCKER_APP_URL" "$DOCKER_VOLUME_NAME" "$DOCKER_APP_NAME"
install_dmg_app "$GHOSTLAB_APP_URL" "$GHOSTLAB_VOLUME_NAME" "$GHOSTLAB_APP_NAME"
install_dmg_app "$SONOS_APP_URL" "$SONOS_VOLUME_NAME" "$SONOS_APP_NAME"
install_bare_pkg "$YUBIKEY_MANAGER_APP_URL" "$YUBIKEY_MANAGER_APP_NAME"
