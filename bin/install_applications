#! /usr/bin/env bash

# Installs applications.

install_dmg_app "$CLEAN_SHOT_APP_URL" "$CLEAN_SHOT_VOLUME_NAME" "$CLEAN_SHOT_APP_NAME"
install_zip_app "$COOLANT_APP_URL" "$COOLANT_APP_NAME"
install_dmg_app "$DOCKER_APP_URL" "$DOCKER_VOLUME_NAME" "$DOCKER_APP_NAME"
install_program "$GIT_FILTER_REPO_APP_URL" "$GIT_FILTER_REPO_APP_NAME"
install_dmg_app "$HAND_BRAKE_CLI_APP_URL" "$HAND_BRAKE_CLI_VOLUME_NAME" "$HAND_BRAKE_CLI_APP_NAME"
install_dmg_app "$IVPN_APP_URL" "$IVPN_VOLUME_NAME" "$IVPN_APP_NAME"
install_dmg_app "$PIXEL_SNAP_APP_URL" "$PIXEL_SNAP_VOLUME_NAME" "$PIXEL_SNAP_APP_NAME"
install_zip_app "$RETROBATCH_HANDLER_APP_URL" "$RETROBATCH_HANDLER_APP_NAME"
install_dmg_app "$RESOLUTIONATOR_APP_URL" "$RESOLUTIONATOR_VOLUME_NAME" "$RESOLUTIONATOR_APP_NAME"
install_dmg_app "$SONOS_APP_URL" "$SONOS_VOLUME_NAME" "$SONOS_APP_NAME"
