#! /usr/bin/env bash

# Installs applications.

install_zip_app "$BIT_WARDEN_APP_URL" "$BIT_WARDEN_APP_NAME"
install_dmg_app "$DOCKER_APP_URL" "$DOCKER_VOLUME_NAME" "$DOCKER_APP_NAME"
install_zip_app "$MONEYWELL_APP_URL" "$MONEYWELL_APP_NAME"
install_git_app "$PGENV_APP_URL" "$PGENV_APP_PATH"
install_dmg_app "$SONOS_APP_URL" "$SONOS_VOLUME_NAME" "$SONOS_APP_NAME"
install_zip_app "$TANA_APP_URL" "$TANA_APP_NAME"
